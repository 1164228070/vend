/**
 * 常用js工具类
 */
var common = function() {};

/**
 * 提示
 * @param msg     提示信息
 */
common.tip = function(msg){
	alert(msg);
};   
   
common.sessionOut = function(){
	//
	 
};

/**
 * 二次编码
 * @param param
 * @returns
 */
common.encodeURI = function(param){
	if(typeof(param)!='undefined'){
		return encodeURI(encodeURI(param));
	}else{
		return '';
	}
}

